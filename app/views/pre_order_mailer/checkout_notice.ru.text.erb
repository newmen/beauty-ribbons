<%= render partial: 'order_mailer/greeting', formats: [:text] %>

Ваш предзаказ №<%= @order.id %> содержит следующие позиции:

<%= render partial: 'order_mailer/products_list', formats: [:text] %>

<%= render partial: 'order_mailer/sign', formats: [:text] %>
