require "spec_helper"

describe CurrentUsersController do
  describe "routing" do
    # these tests doesn't work for some reason :(

    # it "routes to #edit" do
    #   get("/current_users/edit").should route_to("current_users#edit")
    # end

    # it "routes to #update" do
    #   put("/current_users").should route_to("current_users#update")
    # end
  end
end
